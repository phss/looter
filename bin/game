#!/usr/bin/env ruby

require_relative '../lib/looter'
require 'YAML'

adventure_file_path = ARGV.first
adventure_raw = YAML.load_file(adventure_file_path)

adventure = Adventure.from_hash(adventure_raw)

ui = UI.new($stdin, $stdout)

game = Game.new(ui)
game.play(adventure)
